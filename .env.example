# intervals.icu — https://intervals.icu/settings (sección "Developer Settings")
# Sin estas variables la sección "Más allá del código" se muestra sin métricas.
INTERVALS_API_KEY=
# Opcional: id de atleta (ej. i123456). Por defecto "0" = el dueño de la API key.
INTERVALS_ATHLETE_ID=